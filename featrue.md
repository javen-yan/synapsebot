后端优化

1. 上传skills 压缩包强化, 支持 一个压缩包里面多个skill， 自动识别skill文件夹，然后进行导入
2. mcp 接口需要分组，比如 git: git status|git add|git commit|git push ... 等操作，按照mcp类型分组
3. 是否可以支持mcp的动态配置，用户可以编辑 mcp 的配置，比如 mcp 的命令，参数等， 动态加载 mcp tools 类似于reload

前端优化

1. 根据以上后端优化，调整前端的接口调用
